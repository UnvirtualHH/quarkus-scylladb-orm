<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.prgrm.quarkus-scylladb-orm</groupId>
        <artifactId>quarkus-scylladb-orm-parent</artifactId>
        <version>1.0.12</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>quarkus-scylladb-orm</artifactId>
    <name>Quarkus ScyllaDB ORM - Runtime</name>

    <dependencies>
        <!-- The entity annotations and the interfaces generated code implements. -->
        <dependency>
            <groupId>de.prgrm.quarkus-scylladb-orm</groupId>
            <artifactId>quarkus-scylladb-orm-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- The annotation processor. Compile scope, so that adding this one extension
             to an application still generates its mappers and repositories with no extra
             build configuration — javac picks the processor up off the compile classpath.
             The cost is that the processor and JavaPoet also reach the application's
             runtime classpath. Nothing at runtime references them any more (see
             ScyllaDBOrmProcessor#registerBeans), so they are unreachable for a native
             image; taking them out of JVM mode too means moving this to `provided` and
             asking applications to declare the processor themselves, which is a breaking
             change held back for a release that can carry the migration note. -->
        <dependency>
            <groupId>de.prgrm.quarkus-scylladb-orm</groupId>
            <artifactId>quarkus-scylladb-orm-processor</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-core</artifactId>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>

        <dependency>
            <groupId>com.scylladb</groupId>
            <artifactId>java-driver-core</artifactId>
            <version>${scylla-driver.version}</version>
        </dependency>
        <dependency>
            <groupId>com.scylladb</groupId>
            <artifactId>java-driver-query-builder</artifactId>
            <version>${scylla-driver.version}</version>
        </dependency>
        <!-- Micrometer metrics factory (same fork, avoids a second driver core on the
             classpath). Pulls micrometer-core; metrics stay off unless explicitly enabled
             and a MeterRegistry bean is present. -->
        <dependency>
            <groupId>com.scylladb</groupId>
            <artifactId>java-driver-metrics-micrometer</artifactId>
            <version>${scylla-driver.version}</version>
        </dependency>

        <dependency>
            <groupId>io.smallrye.reactive</groupId>
            <artifactId>mutiny</artifactId>
        </dependency>

        <!-- Jakarta Annotations -->
        <dependency>
            <groupId>jakarta.enterprise</groupId>
            <artifactId>jakarta.enterprise.cdi-api</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.inject</groupId>
            <artifactId>jakarta.inject-api</artifactId>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Quarkus extension plugin -->
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-extension-maven-plugin</artifactId>
                <version>${quarkus.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>extension-descriptor</goal>
                        </goals>
                        <configuration>
                            <deployment>${project.groupId}:${project.artifactId}-deployment:${project.version}
                            </deployment>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals>
                            <goal>jandex</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>


            <!-- Generates the Quarkus config documentation for ScyllaOrmConfig. -->
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <configuration>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>io.quarkus</groupId>
                                    <artifactId>quarkus-extension-processor</artifactId>
                                    <version>${quarkus.version}</version>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
